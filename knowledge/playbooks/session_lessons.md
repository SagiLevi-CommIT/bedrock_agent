# Session lessons (playbook stub)

Runtime content for `get_playbook("session_lessons")` is read from
`knowledge/lessons/curated.md` (or `LESSONS_CURATED_PATH` in the container).

Hand-edit curated lessons from raw exports under `knowledge/lessons/raw/`.
